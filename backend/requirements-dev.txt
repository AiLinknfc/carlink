# Dependencias solo para desarrollo/test — no se instalan en producción (el Dockerfile de
# Railway solo usa requirements.txt, ver Dockerfile:14-15). CI instala este archivo además
# del principal para poder correr `pytest tests/`.
pytest==9.1.1
pytest-asyncio==1.4.0a2
pytest-mock==3.15.1
